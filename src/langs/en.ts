import { ITranslateKeys } from "../core/classes/language-support";

const keys: ITranslateKeys = {
	ERROR_COMMAND_ERROR: `An error on the command {command} occured.`,
	SEEK_HELP: `Seek help`,
};

export default keys;
